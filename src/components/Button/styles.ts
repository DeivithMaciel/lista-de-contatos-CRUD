import styled from 'styled-components'

export const ButtonPrimary = styled.button`
  padding: 8px 12px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  background-color: #2f80ed;
  color: #fff;

  &:hover {
    opacity: 0.9;
  }
`
